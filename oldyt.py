#---------------------MIRRORSHADES--------------------------------------------------
# title_regex = re.compile(r'Week (\d+).*Part (\d+)')
# for file in os.listdir():
    # if file.endswith('.mp3'):
        # if title_regex.search(file) is not None:
            # week, part = title_regex.search(file).groups()
            # if len(week)<2:
                # week = '0'*(2-len(week))+week
            # shutil.move(file, os.path.join('RollPlay Mirrorshades','Week {} Part {}.mp3'.format(week, part)))
#---------------------SWAN SONG--------------------------------------------------
# title_regex = re.compile(r'Week (\d+).*Part (\d+)')
# for file in os.listdir():
    # if file.endswith('.mp3'):
        # if title_regex.search(file) is not None:
            # week, part = title_regex.search(file).groups()
            # if len(week)<2:
                # week = '0'*(2-len(week))+week
            # shutil.move(file, os.path.join('RollPlay Swan Song','Week {} Part {}.mp3'.format(week, part)))